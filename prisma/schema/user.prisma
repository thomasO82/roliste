model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  username  String
  password String
  bio String?
  avatar String?
  experience Int @default(0)
  level Int @default(1)
}